name: Run Tests (Mock)

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run mock tests
        run: echo "✅ All tests passed (mock)!"
