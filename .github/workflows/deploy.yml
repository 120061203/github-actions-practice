name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build project (mock)
        run: echo "🛠️ Pretend we are building the project..."

      - name: Deploy (mock)
        run: echo "🚀 Pretend we are deploying the project..."
