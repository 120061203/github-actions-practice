name: Code Lint (Mock)

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run mock linter
        run: echo "🔍 Code looks good (mock)!"
